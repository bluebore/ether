
package baidu.ether;

message RpcMeta {
    optional int64 sequence_id = 1;
    optional string service = 2;
    optional string method = 3;
}

message RpcRequest {
  optional bytes msg = 1;
}

message RpcResponse {
  optional bytes msg = 1;
}

